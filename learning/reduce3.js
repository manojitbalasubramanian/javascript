const arr=[1,2,3]
//syntax: array_name.reduce(function,number)   //number is the starting value
console.log(arr.reduce((a,b)=>a+b,10))  //after reduce function the number is the starting value and after that the array od first number 